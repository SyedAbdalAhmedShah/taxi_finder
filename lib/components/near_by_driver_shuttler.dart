import 'package:flutter/widgets.dart';

class NearByDriverShuttler extends StatelessWidget {
  const NearByDriverShuttler({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
